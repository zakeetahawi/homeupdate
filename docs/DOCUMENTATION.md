# 📚 توثيق شامل للتحديثات - نظام الخواجه

## 📅 تاريخ التحديث: 13 نوفمبر 2025

---

## 📑 جدول المحتويات

1. [نظام العقود الإلكترونية للستائر](#نظام-العقود-الإلكترونية-للستائر)
2. [نظام التركيبات القادمة والمديونيات](#نظام-التركيبات-القادمة-والمديونيات)
3. [الملفات المضافة والمعدلة](#الملفات-المضافة-والمعدلة)
4. [قاعدة البيانات والهجرات](#قاعدة-البيانات-والهجرات)
5. [واجهات المستخدم](#واجهات-المستخدم)
6. [الخطوات التالية](#الخطوات-التالية)

---

## 🎯 نظام العقود الإلكترونية للستائر

### 📋 الوصف
نظام متكامل لإنشاء وإدارة عقود الستائر إلكترونياً مع إمكانية توليد ملفات PDF احترافية.

### ✨ المميزات الرئيسية

#### 1. **إدارة قوالب العقود**
- إنشاء قوالب عقود قابلة للتخصيص
- دعم المتغيرات الديناميكية (اسم العميل، رقم العقد، التاريخ، إلخ)
- تفعيل/تعطيل القوالب
- قالب افتراضي للنظام

#### 2. **إدارة تفاصيل الستائر**
- إضافة عدد غير محدود من الستائر للعقد
- تفاصيل كل ستارة:
  - **الموقع**: (غرفة نوم، صالة، مطبخ، إلخ)
  - **النوع**: (رول، شيش حصيرة، خشب، إلخ)
  - **القياسات**: العرض والارتفاع بالمتر
  - **الأقمشة**: نظام مرن لإضافة مجموعات أقمشة متعددة
    - اسم القماش
    - عدد القطع لكل مجموعة
  - **الإكسسوارات**: قائمة بالإكسسوارات المطلوبة
  - **ملاحظات خاصة**: لكل ستارة

#### 3. **توليد PDF احترافي**
- تصميم عربي احترافي مع دعم RTL
- شعار الشركة في الترويسة
- معلومات العميل والطلب
- جدول تفاصيل الستائر
- شروط وأحكام العقد
- توقيع إلكتروني
- معالجة غير متزامنة باستخدام Celery

#### 4. **التكامل مع النظام**
- ربط مباشر مع الطلبات
- تحديث تلقائي لحالة العقد
- رفع تلقائي إلى Google Drive (اختياري)
- إشعارات للمستخدمين

### 🗂️ الملفات الجديدة

#### Models & Database
```
orders/contract_models.py          - نماذج قوالب العقود وتفاصيل الستائر
orders/migrations/0035_*.py        - إضافة جدول قوالب العقود
orders/migrations/0036_*.py        - إضافة جدول تفاصيل الستائر
orders/migrations/0037_*.py        - إضافة حقل نوع العقد
orders/migrations/0038_*.py        - تعديل حقل نوع العقد
orders/migrations/0039_*.py        - حذف حقل نوع العقد
```

#### Views & Forms
```
orders/contract_views.py           - واجهات إدارة العقود
orders/contract_forms.py           - نماذج إدخال بيانات العقود
orders/contract_admin.py           - لوحة تحكم الإدارة
```

#### Services
```
orders/services/contract_generation_service.py  - خدمة توليد PDF
```

#### Templates
```
orders/templates/orders/contract_curtains_manage.html  - واجهة إدارة ستائر العقد
orders/templates/orders/contract_pdf_template.html     - قالب PDF
```

#### Management Commands
```
orders/management/commands/create_default_contract_template.py  - إنشاء قالب افتراضي
```

### 🔧 التعديلات على الملفات الموجودة

#### orders/models.py
- إضافة حقول العقد للطلب:
  - `contract_number`: رقم العقد
  - `contract_file`: ملف PDF العقد
  - `is_contract_generated`: حالة توليد العقد
  - `is_contract_uploaded_to_drive`: حالة الرفع لـ Drive
- إضافة property `debt_amount` لحساب المديونية

#### orders/views.py
- إضافة views جديدة لإدارة العقود
- تحديث order_detail لعرض معلومات العقد

#### orders/urls.py
- إضافة مسارات العقود:
  - `/orders/<order_number>/contract/curtains/manage/`
  - `/orders/<order_number>/contract/curtains/save/`
  - `/orders/<order_number>/contract/curtains/data/`
  - `/orders/<order_number>/contract/generate/`
  - `/orders/<order_number>/contract/pdf/`

#### orders/tasks.py
- إضافة مهمة `generate_contract_pdf_async` للمعالجة غير المتزامنة

#### orders/forms.py
- تحديث نماذج الطلبات لدعم العقود

### 📊 قاعدة البيانات

#### جدول ContractTemplate
```sql
- id: معرف فريد
- name: اسم القالب
- content: محتوى القالب (HTML)
- is_active: نشط/غير نشط
- is_default: قالب افتراضي
- created_at: تاريخ الإنشاء
- updated_at: تاريخ التحديث
```

#### جدول ContractCurtain
```sql
- id: معرف فريد
- order: ربط بالطلب (ForeignKey)
- location: موقع الستارة
- curtain_type: نوع الستارة
- width: العرض (متر)
- height: الارتفاع (متر)
- fabrics_data: بيانات الأقمشة (JSON)
- accessories: الإكسسوارات
- notes: ملاحظات
- created_at: تاريخ الإنشاء
```

### 🎨 واجهة المستخدم

#### بطاقة معلومات العقد
- تصميم احترافي بتدرج لوني جميل
- عرض:
  - رقم الطلب
  - اسم العميل
  - رقم العقد
  - **رقم الفاتورة** (جديد)
- أزرار:
  - العودة للطلب
  - عرض العقد PDF

#### نموذج إدخال الستائر
- واجهة ديناميكية لإضافة الستائر
- نظام مرن للأقمشة:
  - إضافة مجموعات أقمشة غير محدودة
  - حقل عدد القطع لكل مجموعة
- قوائم منسدلة عادية (بدون Select2)
- تحميل البيانات المحفوظة عند التعديل

---

## 📅 نظام التركيبات القادمة والمديونيات

### 📋 الوصف
نظام شامل لمتابعة التركيبات المجدولة للأيام القادمة مع عرض واضح للمديونيات ومتابعتها.

### ✨ المميزات الرئيسية

#### 1. **الوصول السريع من لوحة الطلبات**
- بطاقة بارزة في صفحة الطلبات الرئيسية
- زرين واضحين:
  - الجدول اليومي
  - التركيبات القادمة + المديونيات
- متاح لجميع من لديهم صلاحية الوصول لقسم الطلبات

#### 2. **محدد التواريخ المحسّن**
- عرض 15 يوم قادم (اليوم + 14 يوم)
- تصميم مضغوط في صف واحد
- تبويبات صغيرة وواضحة
- تحديد تلقائي لليوم الحالي

#### 3. **الفلاتر المتقدمة**
- **فلتر الفرع**: اختيار فرع معين أو جميع الفروع (18 فرع)
- **فلتر البائع**: اختيار بائع معين أو جميع البائعين (73 بائع)
- **فلتر حالة الدفع**:
  - الكل
  - عليها مديونية فقط
  - مدفوعة بالكامل فقط
- زر إعادة تعيين الفلاتر
- تحديث تلقائي عند تغيير أي فلتر

#### 4. **بطاقات التركيبات المحسّنة**
- تصميم مضغوط: 4 بطاقات في الصف (على الشاشات الكبيرة)
- معلومات مختصرة وواضحة:
  - وقت التركيب
  - رقم الطلب
  - اسم العميل
  - رقم الهاتف
  - رقم الفاتورة
  - المبلغ الإجمالي
  - المبلغ المدفوع
  - **المبلغ المتبقي** (بارز للمديونيات)
  - اسم البائع
- شارة ملونة لحالة الدفع:
  - 🔴 أحمر: عليها مديونية
  - 🟢 أخضر: مدفوعة بالكامل

#### 5. **الإحصائيات الفورية**
- إجمالي عدد التركيبات
- إجمالي المديونيات
- إجمالي المبلغ المدفوع
- عدد التركيبات التي عليها مديونية
- تحديث تلقائي حسب الفلاتر المطبقة

#### 6. **الإجراءات السريعة**
- **عرض الطلب**: الانتقال لصفحة تفاصيل الطلب
- **متابعة المديونية**: الانتقال لقسم الدفعات مباشرة (للطلبات التي عليها مديونية)

### 🗂️ الملفات الجديدة

#### Templates
```
installations/templates/installations/upcoming_installations_schedule.html
```

### 🔧 التعديلات على الملفات الموجودة

#### installations/views.py
- إضافة `upcoming_installations_schedule`: عرض صفحة التركيبات القادمة
- إضافة `api_upcoming_installations`: API لجلب التركيبات مع الفلاتر
- إضافة `api_upcoming_installations_filters`: API لجلب قوائم الفروع والبائعين
- إصلاحات:
  - استخدام `Salesperson` model بدلاً من `User` للفلاتر
  - استخدام `get_display_name()` بدلاً من `get_full_name()`
  - استخدام `order_number` بدلاً من `order_id` في الروابط

#### installations/urls.py
- إضافة مسارات جديدة:
  - `/installations/upcoming-installations/`
  - `/installations/api/upcoming-installations/`
  - `/installations/api/upcoming-installations/filters/`

#### orders/templates/orders/orders_dashboard.html
- إضافة بطاقة بارزة للوصول السريع للتركيبات القادمة
- تصميم احترافي بتدرج لوني
- زرين واضحين للجدول اليومي والتركيبات القادمة

### 🎨 تحسينات واجهة المستخدم

#### محدد التواريخ
**قبل:**
- بطاقات كبيرة تأخذ مساحة كبيرة
- عرض في عدة صفوف
- خطوط كبيرة

**بعد:**
- بطاقات مضغوطة (ربع الحجم)
- عرض في صف واحد
- خطوط صغيرة وواضحة
- استخدام أسماء مختصرة للأيام والأشهر

#### بطاقات التركيبات
**قبل:**
- بطاقة واحدة في الصف (عرض كامل)
- معلومات كثيرة ومفصلة
- زر "تفاصيل التركيب"

**بعد:**
- 4 بطاقات في الصف (على الشاشات الكبيرة)
- معلومات مختصرة وأساسية
- إزالة زر "تفاصيل التركيب"
- تركيز على المعلومات المهمة فقط

### 📊 API Endpoints

#### GET `/installations/api/upcoming-installations/`
**Parameters:**
- `date`: التاريخ (YYYY-MM-DD)
- `branch`: معرف الفرع (اختياري)
- `salesperson`: معرف البائع (اختياري)
- `debt_status`: حالة الدفع (with_debt/fully_paid) (اختياري)

**Response:**
```json
{
  "installations": [
    {
      "installation_id": 6060,
      "order_id": 18046,
      "order_number": "16-0177-0001",
      "customer_name": "يوسف رياض الزخاري",
      "customer_phone": "01226349562",
      "location_address": "مصر",
      "scheduled_time": "09:00",
      "invoice_number": "1469",
      "contract_number": "4306",
      "total_amount": 0.0,
      "paid_amount": 0.0,
      "debt_amount": 0.0,
      "salesperson_name": "مينا صلاح",
      "team_name": "فريق التركيب",
      "status_display": "مجدول"
    }
  ],
  "stats": {
    "total_count": 1,
    "total_debt": 0.0,
    "total_paid": 0.0,
    "with_debt_count": 0
  }
}
```

#### GET `/installations/api/upcoming-installations/filters/`
**Response:**
```json
{
  "branches": [
    {"id": 1, "name": "الفرع الرئيسي"},
    {"id": 2, "name": "فرع المعادي"}
  ],
  "salespersons": [
    {"id": 1, "name": "zakee tahawi"},
    {"id": 28, "name": "مينا صلاح ذكى سليمان"}
  ]
}
```

---

## 📁 الملفات المضافة والمعدلة

### ✅ ملفات جديدة (15 ملف)

#### نظام العقود
1. `orders/contract_models.py` - نماذج البيانات
2. `orders/contract_views.py` - واجهات العرض
3. `orders/contract_forms.py` - نماذج الإدخال
4. `orders/contract_admin.py` - لوحة الإدارة
5. `orders/services/contract_generation_service.py` - خدمة توليد PDF
6. `orders/templates/orders/contract_curtains_manage.html` - واجهة الإدارة
7. `orders/templates/orders/contract_pdf_template.html` - قالب PDF
8. `orders/management/commands/create_default_contract_template.py` - أمر إنشاء القالب
9. `orders/migrations/0035_add_contract_templates.py` - هجرة قوالب العقود
10. `orders/migrations/0036_add_contract_curtains.py` - هجرة تفاصيل الستائر
11. `orders/migrations/0037_order_contract_type.py` - هجرة نوع العقد
12. `orders/migrations/0038_alter_order_contract_type.py` - تعديل نوع العقد
13. `orders/migrations/0039_remove_order_contract_type.py` - حذف نوع العقد

#### نظام التركيبات القادمة
14. `installations/templates/installations/upcoming_installations_schedule.html` - واجهة التركيبات القادمة

#### التوثيق
15. `CONTRACTS_SYSTEM_README.md` - توثيق نظام العقود (قديم)
16. `DOCUMENTATION.md` - التوثيق الشامل (هذا الملف)

### 🔄 ملفات معدلة (14 ملف)

#### نظام الطلبات
1. `orders/models.py` - إضافة حقول العقد و debt_amount
2. `orders/views.py` - إضافة views العقود
3. `orders/urls.py` - إضافة مسارات العقود
4. `orders/forms.py` - تحديث نماذج الطلبات
5. `orders/tasks.py` - إضافة مهمة توليد PDF
6. `orders/templatetags/order_extras.py` - إضافة template tags
7. `orders/templates/orders/order_detail.html` - عرض معلومات العقد
8. `orders/templates/orders/order_form.html` - تحديث نموذج الطلب
9. `orders/templates/orders/order_success.html` - صفحة النجاح
10. `orders/templates/orders/orders_dashboard.html` - إضافة بطاقة التركيبات القادمة
11. `orders/templates/orders/installation_orders.html` - تحديثات التركيبات

#### نظام التركيبات
12. `installations/views.py` - إضافة views التركيبات القادمة
13. `installations/urls.py` - إضافة مسارات التركيبات القادمة

#### JavaScript
14. `static/js/order_form_simplified.js` - تحديثات النماذج

---

## 🗄️ قاعدة البيانات والهجرات

### الجداول الجديدة

#### 1. orders_contracttemplate
```sql
CREATE TABLE orders_contracttemplate (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(200) NOT NULL,
    content TEXT NOT NULL,
    is_active BOOLEAN DEFAULT 1,
    is_default BOOLEAN DEFAULT 0,
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL
);
```

#### 2. orders_contractcurtain
```sql
CREATE TABLE orders_contractcurtain (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    order_id INTEGER NOT NULL,
    location VARCHAR(100) NOT NULL,
    curtain_type VARCHAR(100) NOT NULL,
    width DECIMAL(10, 2) NOT NULL,
    height DECIMAL(10, 2) NOT NULL,
    fabrics_data TEXT,
    accessories TEXT,
    notes TEXT,
    created_at DATETIME NOT NULL,
    FOREIGN KEY (order_id) REFERENCES orders_order(id)
);
```

### التعديلات على الجداول الموجودة

#### orders_order
```sql
-- إضافة حقول جديدة
ALTER TABLE orders_order ADD COLUMN contract_number VARCHAR(50);
ALTER TABLE orders_order ADD COLUMN contract_file VARCHAR(100);
ALTER TABLE orders_order ADD COLUMN is_contract_generated BOOLEAN DEFAULT 0;
ALTER TABLE orders_order ADD COLUMN is_contract_uploaded_to_drive BOOLEAN DEFAULT 0;
```

### تشغيل الهجرات
```bash
python manage.py migrate orders 0035
python manage.py migrate orders 0036
python manage.py migrate orders 0037
python manage.py migrate orders 0038
python manage.py migrate orders 0039
```

### إنشاء القالب الافتراضي
```bash
python manage.py create_default_contract_template
```

---

## 🎨 واجهات المستخدم

### 1. صفحة الطلبات الرئيسية
**المسار:** `/orders/dashboard/`

**التحسينات:**
- بطاقة بارزة للتركيبات القادمة
- تدرج لوني احترافي (بنفسجي → أرجواني)
- زرين واضحين:
  - الجدول اليومي
  - التركيبات القادمة + المديونيات

### 2. صفحة إدارة ستائر العقد
**المسار:** `/orders/<order_number>/contract/curtains/manage/`

**المميزات:**
- بطاقة معلومات محسّنة تعرض:
  - رقم الطلب
  - اسم العميل
  - رقم العقد
  - رقم الفاتورة
- نموذج ديناميكي لإضافة الستائر
- نظام مرن للأقمشة
- تحميل البيانات المحفوظة

### 3. صفحة التركيبات القادمة
**المسار:** `/installations/upcoming-installations/`

**المميزات:**
- محدد تواريخ مضغوط (15 يوم)
- فلاتر متقدمة (فرع، بائع، حالة دفع)
- بطاقات مضغوطة (4 في الصف)
- إحصائيات فورية
- إجراءات سريعة

---

## 🚀 الخطوات التالية

### المطلوب تنفيذه

#### 1. تحديث Backend لنظام الأقمشة الديناميكي
- [ ] تحديث `ContractCurtain` model لإضافة `fabrics_data` JSONField
- [ ] إنشاء وتشغيل migration جديدة
- [ ] تحديث `contract_curtains_save` view لمعالجة مصفوفة `fabrics`
- [ ] تحديث `contract_curtains_data` view لإرجاع `fabrics` array
- [ ] تحديث PDF template لعرض مجموعات الأقمشة

#### 2. اختبارات شاملة
- [ ] اختبار توليد العقود لطلبات مختلفة
- [ ] اختبار الفلاتر في صفحة التركيبات القادمة
- [ ] اختبار الأداء مع عدد كبير من التركيبات
- [ ] اختبار التوافق مع المتصفحات المختلفة

#### 3. تحسينات إضافية
- [ ] إضافة إمكانية طباعة العقد مباشرة
- [ ] إضافة إشعارات للتركيبات القادمة
- [ ] إضافة تقارير المديونيات
- [ ] إضافة إحصائيات متقدمة

---

## 📝 ملاحظات مهمة

### نظام العقود
1. يجب تشغيل Celery لتوليد PDF بشكل غير متزامن
2. يجب التأكد من وجود شعار الشركة في المسار الصحيح
3. القالب الافتراضي يُنشأ تلقائياً عند أول تشغيل

### نظام التركيبات القادمة
1. الفلاتر تعمل مع نموذج `Salesperson` وليس `User`
2. يتم عرض 15 يوم قادم فقط
3. البطاقات responsive وتتكيف مع جميع الشاشات

### الأداء
1. استخدام `select_related` و `prefetch_related` لتحسين الأداء
2. المعالجة غير المتزامنة للعمليات الثقيلة
3. التحميل الديناميكي للبيانات عبر AJAX

---

## 👥 المساهمون

- **المطور**: Augment Agent
- **العميل**: zakee tahawi
- **Repository**: https://github.com/zakeetahawi/homeupdate.git

---

## 📞 الدعم

للمساعدة أو الاستفسارات، يرجى فتح issue في GitHub repository.

---

**آخر تحديث:** 13 نوفمبر 2025

