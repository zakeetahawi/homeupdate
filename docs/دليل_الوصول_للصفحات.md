# 🗺️ دليل الوصول للصفحات

## 📄 الصفحات الثلاث المطلوبة

### 1. **modification_detail.html** - تفاصيل طلب التعديل

**الرابط:**
```
http://192.168.1.30:8000/installations/modification/<modification_id>/
```

**مثال:**
```
http://192.168.1.30:8000/installations/modification/1/
http://192.168.1.30:8000/installations/modification/5/
```

**كيف تصل إليها:**
1. اذهب إلى **صفحة تفاصيل التركيب**
2. ابحث عن قسم **"طلبات التعديل"**
3. اضغط على أي طلب تعديل
4. ستفتح صفحة `modification_detail.html`

**أو مباشرة:**
- اذهب لقائمة التركيبات
- اختر تركيب فيه طلب تعديل
- ستجد زر "عرض التعديل" أو "تفاصيل التعديل"

---

### 2. **orders_modal.html** - نافذة الطلبات المنبثقة

**الرابط:**
```
http://192.168.1.30:8000/installations/ajax/orders-modal/<order_type>/
```

**أنواع الطلبات:**
- `ready` - طلبات جاهزة للتركيب
- `needs_scheduling` - تحتاج جدولة
- `scheduled` - مجدولة
- `in_progress` - قيد التنفيذ
- `completed` - مكتملة

**مثال:**
```
http://192.168.1.30:8000/installations/ajax/orders-modal/ready/
http://192.168.1.30:8000/installations/ajax/orders-modal/scheduled/
```

**كيف تصل إليها:**
1. في **لوحة التحكم** (Dashboard)
2. ستجد **بطاقات ملونة** تعرض إحصائيات
3. اضغط على أي بطاقة (مثل "جاهز للتركيب")
4. ستفتح نافذة منبثقة تستخدم `orders_modal.html`

**ملاحظة:** هذا Modal (نافذة منبثقة) وليس صفحة مستقلة

---

### 3. **orders_modal_total.html** - نافذة إجمالي الطلبات

**الحالة:** ❌ **غير مستخدمة حالياً**

هذا الملف موجود لكن لا يوجد view يستخدمه! 
- لا توجد دالة في `views.py` تعرض هذا الملف
- لا توجد روابط تشير إليه

**الحل:**
إما:
1. حذف الملف إذا لم يكن مستخدماً
2. أو البحث في الكود القديم لمعرفة كيف كان يُستخدم

---

## 🧪 كيف تختبر الإصلاحات

### الطريقة 1: اختبار modification_detail
```bash
# افتح المتصفح واذهب إلى:
http://192.168.1.30:8000/installations/modification/1/
```

### الطريقة 2: اختبار orders_modal
```bash
# افتح المتصفح واذهب إلى:
http://192.168.1.30:8000/installations/ajax/orders-modal/ready/
```

### الطريقة 3: من لوحة التحكم
1. اذهب إلى: `http://192.168.1.30:8000/installations/`
2. اضغط على أي بطاقة في الإحصائيات
3. ستفتح النافذة المنبثقة

---

## 🔍 كيف تجد modification_id

**طريقة 1: من قاعدة البيانات**
```bash
cd /home/zakee/homeupdate
source venv/bin/activate
python manage.py shell -c "
from installations.models import ModificationRequest
mods = ModificationRequest.objects.all()[:5]
for m in mods:
    print(f'ID: {m.id} - طلب: {m.installation.order.order_number}')
"
```

**طريقة 2: من Admin**
```
http://192.168.1.30:8000/admin/installations/modificationrequest/
```

---

## ✅ تأكيد الإصلاح

بعد تشغيل السيرفر، جرّب:

```bash
# 1. اختبار currency_format
curl http://192.168.1.30:8000/installations/installation/14262/

# 2. اختبار modification_detail
curl http://192.168.1.30:8000/installations/modification/1/

# 3. اختبار orders_modal
curl http://192.168.1.30:8000/installations/ajax/orders-modal/ready/
```

إذا لم تظهر أخطاء، كل شيء يعمل! ✅

---

## 📝 ملاحظات

- ✅ **تم إصلاح** `currency_format` ليدعم String و Number
- ✅ **تم إضافة** `{% load order_extras %}` لجميع الملفات
- ⚠️ **orders_modal_total.html** غير مستخدم (ربما ملف قديم)

**شغّل السيرفر وجرّب الآن!** 🚀
