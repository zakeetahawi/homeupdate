# صفحة تقرير الطلبات - Orders Report

**التاريخ:** 2025-11-28  
**الموقع:** قسم التقارير

---

## 📊 الميزات الرئيسية

### 1️⃣ **بطاقات إحصائية تفاعلية**
- ✅ بطاقة طلبات الويزارد (لون بنفسجي) 🧙‍♂️
- ✅ بطاقة الطلبات التقليدية (لون وردي) 📋
- ✅ بطاقة إجمالي الطلبات (لون أزرق) 📊
- ✅ كل بطاقة قابلة للنقر للتصفية المباشرة

### 2️⃣ **فلاتر متقدمة**
- 📅 **فلاتر زمنية:**
  - اليوم
  - هذا الأسبوع
  - هذا الشهر
  - الكل
  - تاريخ مخصص (من-إلى)

- 🔍 **فلاتر إضافية:**
  - الفرع
  - البائع
  - نوع الطلب (ويزارد/تقليدي)

### 3️⃣ **جداول تفصيلية**

#### جدول طلبات الويزارد:
| العمود | الوصف | قابل للنقر |
|--------|-------|-----------|
| رقم الطلب | يحول لصفحة مراجعة الويزارد | ✅ |
| رقم العقد | يحول لصفحة عرض العقد | ✅ |
| العميل | يحول لصفحة تفاصيل العميل | ✅ |
| صورة العقد | صورة مصغرة قابلة للنقر | ✅ |
| الفرع | يحول لصفحة تفاصيل الفرع | ✅ |
| البائع | اسم البائع | - |
| نوع الخدمة | شارات ملونة (تسليم، تركيب، إلخ) | - |
| التاريخ | تاريخ الإنشاء | - |
| الحالة | شارة "ويزارد" | - |

#### جدول الطلبات التقليدية:
- نفس الأعمدة والميزات
- شارة "تقليدي" بدلاً من "ويزارد"

---

## 🎨 التصميم

### الألوان:
```css
/* طلبات الويزارد */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* الطلبات التقليدية */
background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);

/* إجمالي */
background: linear-gradient(135deg, #48c6ef 0%, #6f86d6 100%);
```

### شارات الخدمات:
- 🟢 **تسليم** (#16a085)
- 🔵 **تركيب** (#2980b9)
- 🟠 **إكسسوار** (#d35400)
- 🟢 **منتجات** (#27ae60)
- 🔴 **معاينة** (#e74c3c)

---

## 📂 الملفات المنشأة

### 1. Backend:
```
reports/orders_report_views.py
```
**الوظائف:**
- `orders_report_view(request)` - View رئيسي للتقرير
- معالجة الفلاتر (التاريخ، الفرع، البائع، نوع الطلب)
- جلب البيانات من Order و DraftOrder
- حساب الإحصائيات

**الاستعلامات:**
```python
# الطلبات التقليدية (بدون ويزارد)
traditional_orders = Order.objects.filter(date_filter).exclude(
    draft_order__isnull=False
)

# طلبات الويزارد (المكتملة فقط)
wizard_orders = DraftOrder.objects.filter(
    date_filter,
    status='completed'
)
```

### 2. Frontend:
```
reports/templates/reports/orders_report.html
```
**الأقسام:**
- بطاقات الإحصائيات (3 بطاقات)
- قسم الفلاتر (نماذج وأزرار)
- جدول طلبات الويزارد
- جدول الطلبات التقليدية

### 3. URLs:
```python
# reports/urls.py
path('orders/', orders_report_view, name='orders_report'),
```

### 4. Navigation:
```html
<!-- templates/base.html -->
<li class="nav-item">
    <a class="nav-link" href="{% url 'reports:dashboard' %}">
        <i class="fas fa-chart-bar"></i> التقارير
    </a>
    <ul class="dropdown-menu">
        <li><a href="{% url 'reports:orders_report' %}">
            <i class="fas fa-shopping-cart"></i> تقرير الطلبات
        </a></li>
    </ul>
</li>
```

---

## 🔗 الروابط

### الوصول للصفحة:
```
/reports/orders/
```

### الروابط في الجدول:

**طلبات الويزارد:**
- رقم الطلب → `/orders/wizard/review/{draft_id}/`
- رقم العقد → `/orders/order/{order_id}/contract/view/`
- العميل → `/customers/{customer_id}/`
- الفرع → `/branches/{branch_id}/`

**الطلبات التقليدية:**
- رقم الطلب → `/orders/{order_id}/`
- (باقي الروابط مماثلة)

---

## 🎯 حالات الاستخدام

### 1. عرض طلبات اليوم:
```
/reports/orders/?filter=today
```

### 2. عرض طلبات ويزارد فقط:
```
/reports/orders/?order_type=wizard
```

### 3. عرض طلبات فرع معين:
```
/reports/orders/?branch=5
```

### 4. عرض طلبات بائع معين في فترة محددة:
```
/reports/orders/?filter=custom&start_date=2025-11-01&end_date=2025-11-30&salesperson=3
```

---

## ✨ الميزات الإضافية

### 1. **Hover Effects:**
- البطاقات ترتفع عند المرور عليها
- الجداول تتغير خلفيتها
- الصور تتكبر عند المرور

### 2. **Responsive Design:**
- يعمل على جميع الشاشات
- الجداول قابلة للتمرير الأفقي على الشاشات الصغيرة

### 3. **Empty States:**
```html
<div class="no-data">
    <i class="fas fa-inbox"></i>
    <p>لا توجد طلبات في هذه الفترة</p>
</div>
```

---

## 🚀 الاستخدام

### 1. الوصول:
- افتح القائمة الجانبية
- اضغط على "التقارير"
- اختر "تقرير الطلبات"

### 2. التصفية:
- اختر الفترة الزمنية (اليوم، أسبوع، شهر)
- أو حدد تاريخ بداية ونهاية مخصص
- اختر الفرع والبائع (اختياري)
- اختر نوع الطلب (اختياري)
- اضغط "تطبيق الفلاتر"

### 3. التفاعل:
- اضغط على البطاقات للتصفية السريعة
- اضغط على أي رابط في الجدول للانتقال للصفحة المختصة
- اضغط على صور العقود لفتحها في نافذة جديدة

---

## 📝 ملاحظات

1. **الأداء:**
   - استخدام `select_related()` لتقليل الاستعلامات
   - الفلاتر تعمل على مستوى قاعدة البيانات

2. **الأمان:**
   - `@login_required` للحماية
   - جميع الروابط تستخدم `{% url %}`

3. **التوسع المستقبلي:**
   - يمكن إضافة تصدير Excel/PDF
   - يمكن إضافة رسوم بيانية
   - يمكن إضافة فلاتر إضافية (حالة الطلب، نوع العميل)

---

**تم التنفيذ بنجاح!** ✅
