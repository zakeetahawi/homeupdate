# 🎨 نظام تخصيص الويزارد - دليل الاستخدام الشامل

## 📋 نظرة عامة

تم إنشاء نظام تخصيص متكامل لويزارد إنشاء الطلبات يتيح لك التحكم الكامل بجميع الحقول الثابتة والخيارات المتاحة من خلال لوحة تحكم Django Admin.

## 🌟 الميزات الرئيسية

### ✅ ما تم إضافته

1. **نماذج قاعدة البيانات الجديدة:**
   - `WizardFieldOption` - لتخصيص خيارات الحقول (طرق التفصيل، أنواع التركيب، إلخ)
   - `WizardStepConfiguration` - لتخصيص خطوات الويزارد
   - `WizardGlobalSettings` - للإعدادات العامة للويزارد

2. **واجهات إدارة متقدمة:**
   - واجهة سهلة وبديهية لإدارة خيارات الحقول
   - إمكانية إضافة/تعديل/حذف الخيارات بكل سهولة
   - ترتيب الخيارات حسب الأولوية
   - تحديد الخيار الافتراضي
   - إضافة أيقونات وألوان للخيارات

3. **البيانات الافتراضية:**
   - تم إنشاء جميع الخيارات الحالية تلقائياً:
     - 8 طرق تفصيل (حلقات، شريط، كبس، إلخ)
     - 4 أنواع تركيب (جداري، سقفي، بيت ستارة، قضيب)
     - 4 أنواع أقمشة (خفيف، ثقيل، معتم، شفاف)
     - 4 طرق دفع (نقدي، بطاقة، تحويل بنكي، تقسيط)
     - 2 حالة طلب (عادي، VIP)
     - 5 أنواع طلبات (تركيب، تفصيل، إكسسوار، معاينة، منتجات)

## 📂 ملفات النظام الجديدة

```
orders/
├── wizard_customization_models.py      # النماذج الأساسية
├── wizard_customization_admin.py       # واجهات الإدارة
├── wizard_customization_helpers.py     # دوال مساعدة
└── migrations/
    ├── 0061_wizard_customization_models.py        # Migration للنماذج
    └── 0062_wizard_customization_default_data.py  # Migration للبيانات الافتراضية
```

## 🎯 كيفية الاستخدام

### 1️⃣ الوصول إلى لوحة التحكم

1. افتح لوحة تحكم Django Admin
2. انتقل إلى قسم **"الطلبات (Orders)"**
3. ستجد 3 أقسام جديدة:
   - **خيارات حقول الويزارد** (Wizard Field Options)
   - **تخصيص خطوات الويزارد** (Wizard Step Configuration)
   - **الإعدادات العامة للويزارد** (Wizard Global Settings)

### 2️⃣ إدارة خيارات الحقول

#### إضافة خيار جديد:

1. اضغط على "إضافة خيار حقل الويزارد"
2. اختر **نوع الحقل** (مثل: طريقة التفصيل)
3. أدخل **القيمة** بالإنجليزية (مثل: `new_method`)
4. أدخل **الاسم المعروض** بالعربية (مثل: طريقة جديدة)
5. اختياري: أضف أيقونة ولون
6. حدد **الترتيب** (الأصغر يظهر أولاً)
7. احفظ

#### تعديل خيار موجود:

1. اضغط على الخيار من القائمة
2. عدل البيانات المطلوبة
3. احفظ

#### حذف خيار:

1. حدد الخيارات المراد حذفها
2. اختر "حذف العناصر المحددة" من قائمة الإجراءات
3. أكد الحذف

#### تعطيل خيار مؤقتاً:

- بدلاً من الحذف، يمكنك فقط إلغاء تحديد مربع **"نشط"**
- سيختفي الخيار من الواجهة دون حذفه

### 3️⃣ تحديد الخيار الافتراضي

1. افتح الخيار المراد جعله افتراضياً
2. حدد مربع **"افتراضي"**
3. احفظ
4. سيتم إلغاء الافتراضي عن الخيارات الأخرى تلقائياً

### 4️⃣ إدارة خطوات الويزارد

يمكنك تخصيص كل خطوة من خطوات الويزارد:
- تغيير العنوان
- إضافة وصف ونص مساعدة
- إضافة أيقونة
- جعل الخطوة إجبارية أو اختيارية
- تفعيل/تعطيل الخطوة

### 5️⃣ الإعدادات العامة

في قسم **الإعدادات العامة للويزارد**، يمكنك التحكم في:

#### ⚙️ الإعدادات العامة:
- تفعيل/تعطيل نظام الويزارد
- تفعيل الحفظ التلقائي للمسودات
- مدة صلاحية المسودات (بالأيام)

#### 💰 إعدادات الدفع:
- نسبة الحد الأدنى للدفع (افتراضي 50%)
- السماح بدفع مبلغ أكبر من الإجمالي

#### 📄 إعدادات العقد:
- أنواع الطلبات التي تتطلب عقد
- تفعيل العقد الإلكتروني
- تفعيل رفع ملفات PDF

#### 🔍 إعدادات المعاينة:
- أنواع الطلبات التي تتطلب معاينة
- السماح بمقاسات طرف العميل

#### 🔔 إعدادات الإشعارات:
- إرسال إشعار عند إنشاء مسودة
- إرسال إشعار عند إنشاء طلب نهائي

#### 🎨 إعدادات العرض:
- إظهار شريط التقدم
- لون السمة الأساسي

## 📝 أمثلة عملية

### مثال 1: إضافة طريقة تفصيل جديدة

```
نوع الحقل: طريقة التفصيل
القيمة: wave_pleat
الاسم المعروض: كسرة موجية
الأيقونة: fas fa-water
اللون: info
الترتيب: 9
نشط: ✓
افتراضي: ✗
```

### مثال 2: إضافة طريقة دفع جديدة

```
نوع الحقل: طريقة الدفع
القيمة: digital_wallet
الاسم المعروض: محفظة إلكترونية
الأيقونة: fas fa-wallet
اللون: success
الترتيب: 5
نشط: ✓
افتراضي: ✗
```

## 🔧 للمطورين

### استخدام الدوال المساعدة:

```python
from orders.wizard_customization_helpers import (
    get_wizard_field_choices,
    get_wizard_field_default,
    get_wizard_settings
)

# الحصول على خيارات طرق التفصيل
tailoring_choices = get_wizard_field_choices('tailoring_type')
# النتيجة: [('rings', 'حلقات'), ('tape', 'شريط'), ...]

# الحصول على القيمة الافتراضية
default_tailoring = get_wizard_field_default('tailoring_type')
# النتيجة: 'rings'

# الحصول على إعدادات الويزارد
settings = get_wizard_settings()
minimum_payment = settings.minimum_payment_percentage
# النتيجة: Decimal('50.00')
```

### تحديث نماذج Django تلقائياً:

```python
from orders.wizard_customization_helpers import auto_update_wizard_form_fields

class MyForm(forms.Form):
    tailoring_type = forms.ChoiceField()
    payment_method = forms.ChoiceField()
    
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        # تحديث جميع الحقول تلقائياً
        auto_update_wizard_form_fields(self)
```

## 🚀 خطط مستقبلية

- [ ] إضافة دعم متعدد اللغات للخيارات
- [ ] إمكانية استيراد/تصدير الخيارات
- [ ] إنشاء قوالب جاهزة للإعدادات
- [ ] واجهة مرئية لتخصيص ألوان وأيقونات الخطوات
- [ ] نظام صلاحيات متقدم للتحكم بالخيارات

## ⚠️ ملاحظات هامة

1. **لا تحذف الخيارات المستخدمة في طلبات موجودة** - قد يؤدي ذلك لأخطاء في البيانات
2. **استخدم التعطيل بدلاً من الحذف** - للخيارات التي لم تعد تستخدمها
3. **القيمة يجب أن تكون فريدة** - لكل نوع حقل
4. **القيم بالإنجليزية فقط** - استخدم أحرف، أرقام، `_` و `-` فقط
5. **الترتيب مهم** - الأرقام الأصغر تظهر أولاً

## 📞 الدعم

للمساعدة أو الإبلاغ عن مشاكل:
- راجع الوثائق أعلاه
- تحقق من سجلات الأخطاء في Django Admin
- اتصل بمدير النظام

---

**تم إنشاء النظام بنجاح! 🎉**

التاريخ: نوفمبر 2025
الإصدار: 1.0
