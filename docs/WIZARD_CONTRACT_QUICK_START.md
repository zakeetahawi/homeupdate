# نظام الويزارد والعقود - دليل البدء السريع
**Quick Start Guide - Wizard & Contract System**

---

## ⚡ التطبيق السريع

### الخطوة 1: تطبيق المايجريشن
```bash
cd /home/zakee/homeupdate
source venv/bin/activate
python manage.py migrate orders 0050_add_fabric_notes_and_cleanup
```

### الخطوة 2: إعادة تشغيل الخادم
```bash
sudo systemctl restart curtain_system
# أو
sudo systemctl restart gunicorn
```

### الخطوة 3: التحقق
- افتح المتصفح: `http://your-server-ip/orders/wizard/start/`
- قم بإنشاء طلب تجريبي
- أضف ستارة مع أقمشة وإكسسوارات
- تحقق من ظهور الملاحظات في العقد

---

## 📋 ما الجديد؟

### ✅ تم التنفيذ

1. **حقل ملاحظات للأقمشة** ⭐
   - حقل جديد: `CurtainFabric.notes`
   - يظهر في قالب العقد
   - يدعم في الويزارد (step 5)

2. **تأكيد ملاحظات الإكسسوارات** ✅
   - الحقل موجود: `CurtainAccessory.notes`
   - يظهر بشكل صحيح في العقد

3. **حذف النظام القديم** 🗑️
   - حذف `DynamicContractTemplate`
   - حذف `TemplateSection`
   - حذف `TemplateSectionField`
   - تنظيف لوحة التحكم

4. **التكامل الكامل** 🔗
   - الويزارد ↔️ قاعدة البيانات
   - الويزارد ↔️ العقود
   - الويزارد ↔️ لوحة التحكم

5. **التوثيق الشامل** 📚
   - `WIZARD_CONTRACT_COMPLETE_DOCUMENTATION.md` - توثيق كامل
   - `WIZARD_CONTRACT_TECHNICAL_GUIDE.md` - دليل تقني
   - `WIZARD_CONTRACT_QUICK_START.md` - هذا الملف

---

## 🎯 الاستخدام السريع

### إنشاء طلب جديد

1. **الدخول للويزارد**: `/orders/wizard/start/`

2. **الخطوة 1**: إدخال بيانات العميل
   - اختر العميل
   - اختر الفرع
   - اختر البائع
   - أضف ملاحظات (اختياري)

3. **الخطوة 2**: اختر نوع الطلب
   - تركيب / تفصيل / إكسسوار / إلخ

4. **الخطوة 3**: أضف عناصر الطلب
   - أضف المنتجات / الأقمشة / الإكسسوارات
   - حدد الكميات والأسعار

5. **الخطوة 4**: تفاصيل الفاتورة والدفع
   - أدخل أرقام الفواتير والعقود
   - حدد طريقة الدفع والمبلغ المدفوع

6. **الخطوة 5**: العقد الإلكتروني (اختياري)
   - أضف ستارة
   - حدد اسم الغرفة والمقاسات
   - **أضف أقمشة مع ملاحظات** ⭐
   - **أضف إكسسوارات مع ملاحظات** ✅
   - أو ارفع ملف PDF

7. **الخطوة 6**: مراجعة وتأكيد
   - راجع جميع البيانات
   - اضغط "إنشاء الطلب"

### عرض العقد

```
/orders/order/<order_id>/contract/
```

### تحميل العقد كـ PDF

```
/orders/order/<order_id>/contract/pdf/
```

---

## 🔍 أمثلة عملية

### مثال 1: إضافة ستارة بقماشين

```javascript
// في الويزارد - الخطوة 5
1. اضغط "إضافة ستارة"
2. أدخل البيانات:
   - اسم الغرفة: "غرفة المعيشة"
   - العرض: 3.5 م
   - الطول: 2.8 م
   - نوع التركيب: "حائط - جبس"
   
3. أضف القماش الأول:
   - النوع: "خفيف"
   - اختر القماش من الفاتورة
   - عدد الأمتار: 10
   - عدد القطع: 2
   - نوع التفصيل: "شريط"
   - ✅ ملاحظات: "قماش شيفون شفاف - اللون أبيض"
   
4. أضف القماش الثاني:
   - النوع: "ثقيل"
   - اختر القماش من الفاتورة
   - عدد الأمتار: 8
   - عدد القطع: 1
   - نوع التفصيل: "حلقات"
   - ✅ ملاحظات: "قماش مخمل - عازل للضوء"
   
5. أضف إكسسوار:
   - الاسم: "مجرى ألومنيوم"
   - الكمية: 1
   - اللون: "فضي"
   - ✅ ملاحظات: "مجرى مزدوج بطول 3.5 متر"
   
6. احفظ الستارة
```

### مثال 2: تحرير ستارة موجودة

```javascript
1. في قائمة الستائر، اضغط "تحرير"
2. عدّل البيانات المطلوبة
3. عدّل الأقمشة أو الإكسسوارات
4. ⭐ عدّل الملاحظات
5. احفظ التعديلات
```

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: "الكمية المطلوبة أكبر من المتاح"
**الحل**: 
- تحقق من الكمية المتاحة في الفاتورة (الخطوة 3)
- قلل عدد الأمتار في القماش
- أو زد الكمية في الفاتورة

### المشكلة 2: الملاحظات لا تظهر في العقد
**الحل**:
```bash
# تأكد من تطبيق المايجريشن
python manage.py migrate orders 0050

# امسح الكاش
python manage.py clear_cache  # إذا كان موجوداً

# أعد تشغيل الخادم
sudo systemctl restart curtain_system
```

### المشكلة 3: خطأ عند إضافة ستارة
**الحل**:
- تحقق من JavaScript Console (F12)
- تأكد من ملء جميع الحقول المطلوبة
- تأكد من وجود أقمشة في الفاتورة

### المشكلة 4: العقد لا يظهر الستائر
**الحل**:
- تأكد من إضافة الستائر في الخطوة 5
- تأكد من الحفظ قبل الانتقال للخطوة 6
- تحقق من `ContractCurtain.order` في قاعدة البيانات

---

## 📊 قاعدة البيانات

### جداول جديدة/محدثة

```sql
-- تحديث جدول أقمشة الستائر
ALTER TABLE orders_curtainfabric 
ADD COLUMN notes TEXT;

-- حذف الجداول القديمة
DROP TABLE IF EXISTS orders_templatesectionfield;
DROP TABLE IF EXISTS orders_templatesection;
DROP TABLE IF EXISTS orders_dynamiccontracttemplate;
```

### التحقق من البيانات

```sql
-- عرض الأقمشة مع الملاحظات
SELECT 
    cf.id,
    cc.room_name,
    cf.fabric_type,
    cf.fabric_name,
    cf.meters,
    cf.notes  -- ⭐ الحقل الجديد
FROM orders_curtainfabric cf
JOIN orders_contractcurtain cc ON cf.curtain_id = cc.id
WHERE cf.notes IS NOT NULL AND cf.notes != '';

-- عرض الإكسسوارات مع الملاحظات
SELECT 
    ca.id,
    cc.room_name,
    ca.accessory_name,
    ca.quantity,
    ca.color,
    ca.notes  -- ✅ موجود مسبقاً
FROM orders_curtainaccessory ca
JOIN orders_contractcurtain cc ON ca.curtain_id = cc.id
WHERE ca.notes IS NOT NULL AND ca.notes != '';
```

---

## 🔐 الصلاحيات

### الأدوار المدعومة

| الدور | الصلاحيات |
|------|-----------|
| مدير نظام | جميع الصلاحيات |
| مدير عام | جميع المسودات والطلبات |
| مدير منطقة | مسودات وطلبات فروعه فقط |
| مستخدم عادي | مسوداته فقط |

### التحقق من الصلاحيات

```python
# في Django Shell
python manage.py shell

>>> from django.contrib.auth.models import User
>>> user = User.objects.get(username='your_username')
>>> user.groups.all()  # عرض المجموعات
>>> user.has_perm('orders.view_order')  # التحقق من صلاحية
```

---

## 📱 الواجهة

### الويزارد
- تصميم متجاوب (Bootstrap 5)
- دعم كامل للعربية (RTL)
- شريط تقدم واضح
- رسائل خطأ واضحة

### العقد
- تصميم احترافي للطباعة
- دعم PDF
- ألوان أنيقة
- خط Cairo العربي

---

## 🔄 التحديثات المستقبلية

### مخطط له:
- [ ] حذف تلقائي للمسودات القديمة (> 30 يوم)
- [ ] دعم القوالب المخصصة للعقود
- [ ] تصدير العقود بصيغ متعددة (Word, Excel)
- [ ] إشعارات تلقائية عند إنشاء الطلب
- [ ] نسخ احتياطي تلقائي للمسودات

---

## 📞 الدعم

### الملفات المرجعية:
- `WIZARD_CONTRACT_COMPLETE_DOCUMENTATION.md` - التوثيق الكامل
- `WIZARD_CONTRACT_TECHNICAL_GUIDE.md` - الدليل التقني
- `WIZARD_ORDER_CREATION_README.md` - توثيق الويزارد الأساسي
- `COMPLETE_FABRIC_GUIDE.md` - دليل نظام الأقمشة

### الأكواد المصدرية:
- `orders/wizard_models.py`
- `orders/wizard_views.py`
- `orders/wizard_forms.py`
- `orders/contract_models.py`
- `orders/contract_views.py`
- `orders/contract_admin.py`
- `orders/templates/orders/contract_template.html`

---

## ✅ قائمة التحقق

قبل البدء، تأكد من:
- [x] تطبيق المايجريشن `0050_add_fabric_notes_and_cleanup`
- [x] إعادة تشغيل الخادم
- [x] اختبار إنشاء طلب جديد
- [x] اختبار إضافة ستارة مع ملاحظات
- [x] اختبار عرض العقد
- [x] اختبار طباعة العقد

---

## 🎉 الخلاصة

النظام الآن جاهز بالكامل مع:
- ✅ حقل ملاحظات للأقمشة (جديد)
- ✅ حقل ملاحظات للإكسسوارات (موجود مسبقاً)
- ✅ عرض الملاحظات في العقد
- ✅ حذف النظام القديم
- ✅ تكامل كامل
- ✅ توثيق شامل

**جاهز للإنتاج!** 🚀

---

**آخر تحديث**: 2025-11-22  
**الإصدار**: 2.0
