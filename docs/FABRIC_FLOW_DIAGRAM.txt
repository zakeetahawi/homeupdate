═══════════════════════════════════════════════════════════════════════════════
              نظام اختيار الأقمشة في الويزارد - المسار الكامل
═══════════════════════════════════════════════════════════════════════════════

1. إنشاء الطلب (Step 3 - Order Items)
   ┌────────────────────────────────────────┐
   │   إضافة عناصر الفاتورة (أقمشة)       │
   │                                        │
   │   • قماش شانيل - 100 متر              │
   │   • قماش بلاك أوت - 50 متر            │
   │   • قماش دانتيل - 75 متر              │
   └─────────────────┬──────────────────────┘
                     │
                     ▼
2. العقد الإلكتروني (Step 5 - Contract)
   ┌────────────────────────────────────────┐
   │   إضافة ستارة جديدة                   │
   │                                        │
   │   [نوع القماش: خفيف ▼]                │
   │   [اسم القماش ▼]                       │
   │   ├─ قماش شانيل - متوفر: 100 متر      │
   │   ├─ قماش بلاك أوت - متوفر: 50 متر    │
   │   └─ قماش دانتيل - متوفر: 75 متر      │
   └─────────────────┬──────────────────────┘
                     │
                     ▼
3. اختيار القماش
   ┌────────────────────────────────────────┐
   │   القماش المختار: قماش شانيل           │
   │                                        │
   │   💡 المتبقي للاختيار: 100 متر        │
   │                                        │
   │   الكمية: [____] متر                   │
   └─────────────────┬──────────────────────┘
                     │
                     ▼
4. التحقق من الكمية (JavaScript)
   ┌────────────────────────────────────────┐
   │   if (meters > remaining) {            │
   │     ❌ خطأ: الكمية أكبر من المتوفر     │
   │     "المطلوب: 120م > المتاح: 100م"     │
   │   } else {                             │
   │     ✅ إضافة القماش للقائمة المؤقتة    │
   │   }                                    │
   └─────────────────┬──────────────────────┘
                     │
                     ▼
5. حفظ الستارة (AJAX Request)
   ┌────────────────────────────────────────┐
   │   POST /wizard/add-curtain/            │
   │                                        │
   │   {                                    │
   │     room_name: "غرفة النوم",           │
   │     width: 3.5,                        │
   │     height: 2.8,                       │
   │     fabrics: [                         │
   │       {                                │
   │         type: "light",                 │
   │         item_id: 123,  ← ربط بالفاتورة│
   │         meters: 45,                    │
   │         pieces: 2                      │
   │       }                                │
   │     ]                                  │
   │   }                                    │
   └─────────────────┬──────────────────────┘
                     │
                     ▼
6. المعالجة في Django (wizard_views.py)
   ┌────────────────────────────────────────┐
   │   def wizard_add_curtain(request):     │
   │                                        │
   │   • الحصول على المسودة                 │
   │   • إنشاء ContractCurtain              │
   │   • لكل قماش:                          │
   │     ├─ الحصول على order_item           │
   │     ├─ إنشاء CurtainFabric             │
   │     ├─ fabric.full_clean() ← التحقق    │
   │     └─ fabric.save()                   │
   │                                        │
   │   ✅ نجح الحفظ                         │
   └─────────────────┬──────────────────────┘
                     │
                     ▼
7. التحقق في النموذج (CurtainFabric.clean)
   ┌────────────────────────────────────────┐
   │   used_total = CurtainFabric           │
   │     .filter(order_item=self.order_item)│
   │     .aggregate(Sum('meters'))          │
   │                                        │
   │   available = order_item.quantity      │
   │             - used_total               │
   │                                        │
   │   if self.meters > available:          │
   │     ❌ ValidationError                 │
   │   else:                                │
   │     ✅ حفظ البيانات                    │
   └─────────────────┬──────────────────────┘
                     │
                     ▼
8. عرض في الواجهة
   ┌────────────────────────────────────────┐
   │   ╔════════════════════════════════╗   │
   │   ║ 🏠 غرفة النوم                 ║   │
   │   ╠════════════════════════════════╣   │
   │   ║ المقاسات: 3.5م × 2.8م         ║   │
   │   ║                                ║   │
   │   ║ الأقمشة:                       ║   │
   │   ║ • خفيف: قماش شانيل - 45م      ║   │
   │   ║                                ║   │
   │   ║ [حذف]                          ║   │
   │   ╚════════════════════════════════╝   │
   └────────────────────────────────────────┘

9. توليد PDF
   ┌────────────────────────────────────────┐
   │   contract_pdf_template.html           │
   │                                        │
   │   {% if curtain.fabrics.exists %}      │
   │     <!-- النظام الجديد -->             │
   │     {% for fabric in curtain.fabrics %}│
   │       {{ fabric.order_item.product }}  │
   │       {{ fabric.meters }}م             │
   │     {% endfor %}                       │
   │   {% else %}                           │
   │     <!-- النظام القديم -->             │
   │     {{ curtain.light_fabric }}         │
   │   {% endif %}                          │
   └────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════
                              تتبع الكميات
═══════════════════════════════════════════════════════════════════════════════

fabricUsageTracker = {
  '123': {                    // item_id
    total: 100,               // من الفاتورة
    used: 0,                  // البداية
    name: 'قماش شانيل'
  }
}

                    ▼ إضافة ستارة 1 (45 متر)
fabricUsageTracker['123'].used = 45
المتبقي: 100 - 45 = 55 متر

                    ▼ إضافة ستارة 2 (30 متر)
fabricUsageTracker['123'].used = 75
المتبقي: 100 - 75 = 25 متر

                    ▼ محاولة إضافة ستارة 3 (40 متر)
❌ خطأ: 40 > 25
"الكمية المطلوبة (40 متر) أكبر من المتوفر (25 متر)"

═══════════════════════════════════════════════════════════════════════════════
