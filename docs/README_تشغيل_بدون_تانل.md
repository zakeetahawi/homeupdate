# 🚀 تشغيل النظام بدون Cloudflare Tunnel

## ⚡ البدء السريع

```bash
cd /home/zakee/homeupdate
./لينكس/run-production-no-tunnel.sh
```

## 🌐 عناوين الوصول

بعد التشغيل، يمكنك الوصول للنظام عبر:

```
محلي (من نفس الجهاز):
http://localhost:8000

من الشبكة المحلية:
http://192.168.1.30:8000
```

## 🔐 بيانات الدخول

```
المستخدم: admin
كلمة المرور: admin123
```

⚠️ **مهم:** غيّر كلمة المرور فوراً بعد أول تسجيل دخول!

## ✨ المميزات

✅ **أداء عالي** - بدون طبقات وسيطة
✅ **استقرار ممتاز** - لا يعتمد على الإنترنت  
✅ **أمان محلي** - لا تعرض للإنترنت
✅ **سهل الصيانة** - تكوين بسيط

## 📊 الخدمات النشطة

عند التشغيل، يعمل النظام الخدمات التالية:

- ✅ **خادم الويب (Gunicorn)** - على المنفذ 8000
- ✅ **Celery Worker** - المهام الخلفية
- ✅ **Celery Beat** - المهام الدورية
- ✅ **Redis/Valkey** - التخزين المؤقت
- ✅ **النسخ الاحتياطي** - تلقائي كل ساعة

## 🔍 مراقبة النظام

### السجلات المباشرة:

```bash
# Celery Worker (المهام الخلفية)
tail -f /home/zakee/homeupdate/logs/celery_worker.log

# Celery Beat (المهام الدورية)
tail -f /home/zakee/homeupdate/logs/celery_beat.log

# النسخ الاحتياطي
tail -f /home/zakee/homeupdate/logs/db_backup.log
```

### فحص الحالة:

```bash
# فحص جميع الخدمات
ps aux | grep -E 'gunicorn|celery|redis'

# فحص قاعدة البيانات
python manage.py monitor_db --once
```

## 🛑 إيقاف النظام

اضغط `Ctrl+C` في نافذة الترمنال حيث يعمل النظام.

سيتم إيقاف جميع الخدمات تلقائياً:
- ✓ خادم الويب
- ✓ Celery Worker
- ✓ Celery Beat
- ✓ النسخ الاحتياطي

## 🔄 إعادة التشغيل

```bash
# إيقاف النظام (Ctrl+C)
# ثم تشغيله مرة أخرى
./لينكس/run-production-no-tunnel.sh
```

## 🆚 الفرق مع النسخة مع Tunnel

| الميزة | بدون Tunnel | مع Tunnel |
|--------|-------------|-----------|
| الوصول من الإنترنت | ❌ | ✅ |
| الأداء | ⚡⚡⚡ | ⚡⚡ |
| HTTPS | ❌ | ✅ |
| الاستقرار | ⭐⭐⭐ | ⭐⭐ |

## 📱 الوصول من أجهزة أخرى في الشبكة

### من الكمبيوتر:
```
افتح المتصفح واذهب إلى:
http://192.168.1.30:8000
```

### من الموبايل:
```
1. تأكد أن الموبايل على نفس الشبكة (WiFi)
2. افتح المتصفح
3. اذهب إلى: http://192.168.1.30:8000
```

## 🔧 استكشاف الأخطاء

### المشكلة: لا يمكن الوصول من جهاز آخر

**الحل:**
```bash
# 1. تأكد أن النظام يعمل
ps aux | grep gunicorn

# 2. تأكد أن جدار الحماية يسمح بالاتصال
sudo ufw allow 8000/tcp

# 3. تأكد من IP السيرفر
ip addr show | grep "inet "
```

### المشكلة: النظام بطيء

**الحل:**
```bash
# فحص استخدام الموارد
htop

# فحص الاستعلامات البطيئة
tail -f logs/slow_queries.log

# إعادة تشغيل النظام
# Ctrl+C ثم
./لينكس/run-production-no-tunnel.sh
```

### المشكلة: خطأ في قاعدة البيانات

**الحل:**
```bash
# تطبيق الترحيلات
source venv/bin/activate
python manage.py migrate

# فحص الحالة
python manage.py monitor_db --once
```

## 📚 الوثائق الإضافية

- [دليل التشغيل الكامل](./دليل_تشغيل_الإنتاج.md)
- [مقارنة طرق التشغيل](./مقارنة_طرق_التشغيل.md)

## 💡 نصائح

1. **استخدم VPN** للوصول من خارج الموقع بدلاً من فتح منافذ الراوتر
2. **راقب السجلات** بشكل دوري للتأكد من عدم وجود مشاكل
3. **اختبر النسخ الاحتياطي** شهرياً للتأكد من عملها
4. **حدّث النظام** بانتظام للحصول على آخر التحسينات

## 🎯 متى تستخدم هذه الطريقة؟

✅ **مناسب لـ:**
- المكاتب والشركات الصغيرة
- الاستخدام الداخلي فقط
- عندما يكون الأداء أولوية
- الشبكات المحلية الآمنة

❌ **غير مناسب لـ:**
- العمل عن بعد
- الفرق الموزعة جغرافياً
- العملاء الخارجيين

**للوصول من الإنترنت، استخدم:**
```bash
./لينكس/run-production.sh
```

---

**آخر تحديث:** ديسمبر 2025
