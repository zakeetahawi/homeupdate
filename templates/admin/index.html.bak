{% extends "admin/base_site.html" %}
{% load i18n admin_urls static admin_list %}
{% block extrahead %}
    {{ block.super }}
    <style>
    /* تنسيق عام للصفحة */
    #content-main {
        background: #f5f7fa;
        padding: 30px;
        border-radius: 15px;
        min-height: 80vh;
    }
    
    /* إحصائيات سريعة */
    .dashboard-stats {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 20px;
        margin-bottom: 30px;
        padding: 20px 0;
    }
    
    .stat-card {
        background: linear-gradient(135deg, #5e4fa2 0%, #3f2b96 100%);
        color: white;
        padding: 20px;
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.15);
        text-align: center;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        border: 2px solid rgba(255,255,255,0.2);
    }
    
    .stat-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 25px rgba(0,0,0,0.25);
    }
    
    .stat-card.blue { background: linear-gradient(135deg, #1565c0 0%, #0d47a1 100%); }
    .stat-card.green { background: linear-gradient(135deg, #2e7d32 0%, #1b5e20 100%); }
    .stat-card.orange { background: linear-gradient(135deg, #d84315 0%, #bf360c 100%); }
    .stat-card.purple { background: linear-gradient(135deg, #5e4fa2 0%, #3f2b96 100%); }
    
    .stat-card .icon {
        font-size: 2.5em;
        margin-bottom: 10px;
        text-shadow: 0 2px 4px rgba(0,0,0,0.3);
    }
    
    .stat-card .value {
        font-size: 2em;
        font-weight: bold;
        margin: 10px 0;
        text-shadow: 0 2px 4px rgba(0,0,0,0.3);
    }
    
    .stat-card .label {
        font-size: 0.9em;
        opacity: 0.95;
        text-shadow: 0 1px 2px rgba(0,0,0,0.2);
    }
    
    /* تصميم البطاقات للتطبيقات */
    .app-list {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
        gap: 25px;
        margin-top: 20px;
    }
    
    .module {
        background: white;
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        overflow: hidden;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        border: 1px solid #e0e0e0;
    }
    
    .module:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 25px rgba(0,0,0,0.15);
        border-color: #bdbdbd;
    }
    
    .module h2 {
        margin: 0;
        padding: 0;
    }
    
    .module h2 a {
        display: block;
        padding: 20px;
        background: linear-gradient(135deg, #5e4fa2 0%, #3f2b96 100%);
        color: white !important;
        text-decoration: none;
        font-size: 1.1em;
        font-weight: bold;
        transition: background 0.3s ease;
        text-shadow: 0 2px 4px rgba(0,0,0,0.2);
    }
    
    .module h2 a:hover {
        background: linear-gradient(135deg, #4a3f85 0%, #2d1f6e 100%);
    }
    
    /* ألوان مختلفة للمجموعات - أغمق وأكثر وضوحاً */
    .module:nth-child(6n+1) h2 a { background: linear-gradient(135deg, #5e4fa2 0%, #3f2b96 100%); }
    .module:nth-child(6n+2) h2 a { background: linear-gradient(135deg, #c2185b 0%, #880e4f 100%); }
    .module:nth-child(6n+3) h2 a { background: linear-gradient(135deg, #1565c0 0%, #0d47a1 100%); }
    .module:nth-child(6n+4) h2 a { background: linear-gradient(135deg, #2e7d32 0%, #1b5e20 100%); }
    .module:nth-child(6n+5) h2 a { background: linear-gradient(135deg, #d84315 0%, #bf360c 100%); }
    .module:nth-child(6n+6) h2 a { background: linear-gradient(135deg, #00838f 0%, #004d40 100%); }
    
    .module table {
        width: 100%;
        border-collapse: collapse;
        background: white;
    }
    
    .module table thead {
        display: none;
    }
    
    .module table tbody tr {
        border-bottom: 1px solid #e0e0e0;
        transition: background 0.2s ease;
    }
    
    .module table tbody tr:last-child {
        border-bottom: none;
    }
    
    .module table tbody tr:hover {
        background: #f5f5f5;
    }
    
    .module table td {
        padding: 12px 20px;
        color: #424242;
    }
    
    .module table td:first-child {
        font-weight: 600;
        color: #212121;
    }
    
    .module table td:last-child {
        text-align: left;
    }
    
    .module table a {
        display: inline-block;
        padding: 8px 16px;
        margin: 0 5px;
        border-radius: 6px;
        text-decoration: none;
        font-size: 0.85em;
        transition: all 0.2s ease;
        font-weight: 600;
    }
    
    .module table a.changelink {
        background: linear-gradient(135deg, #1565c0 0%, #0d47a1 100%);
        color: white;
        box-shadow: 0 2px 8px rgba(21,101,192,0.15);
    }
    
    .module table a.changelink:hover {
        background: linear-gradient(135deg, #0d47a1 0%, #01579b 100%);
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(21,101,192,0.25);
    }
    
    .module table a.addlink {
        background: linear-gradient(135deg, #2e7d32 0%, #1b5e20 100%);
        color: white;
        box-shadow: 0 2px 8px rgba(46,125,50,0.15);
    }
    
    .module table a.addlink:hover {
        background: linear-gradient(135deg, #1b5e20 0%, #0d3d11 100%);
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(46,125,50,0.25);
    }
    
    /* تحسينات RTL */
    [dir="rtl"] .module table td:last-child {
        text-align: right;
    }
    </style>
{% endblock %}
{% block content %}
    <div id="content-main">
        <!-- لوحة الإحصائيات السريعة -->
        {% if total_customers or total_orders or total_products %}
            <div class="dashboard-stats">
                {% if total_customers %}
                    <div class="stat-card purple">
                        <div class="icon">👥</div>
                        <div class="value">{{ total_customers|default:0 }}</div>
                        <div class="label">إجمالي العملاء</div>
                    </div>
                {% endif %}
                {% if total_orders %}
                    <div class="stat-card blue">
                        <div class="icon">📦</div>
                        <div class="value">{{ total_orders|default:0 }}</div>
                        <div class="label">إجمالي الطلبات</div>
                    </div>
                {% endif %}
                {% if pending_orders %}
                    <div class="stat-card orange">
                        <div class="icon">⏳</div>
                        <div class="value">{{ pending_orders|default:0 }}</div>
                        <div class="label">الطلبات قيد التنفيذ</div>
                    </div>
                {% endif %}
                {% if total_products %}
                    <div class="stat-card green">
                        <div class="icon">📊</div>
                        <div class="value">{{ total_products|default:0 }}</div>
                        <div class="label">المنتجات المتاحة</div>
                    </div>
                {% endif %}
            </div>
        {% endif %}
        <!-- قائمة التطبيقات المجمعة -->
        {% if app_list %}
            <div class="app-list">
                {% for app in app_list %}
                    <div class="module">
                        <h2>
                            <a href="{{ app.app_url }}"
                               class="section"
                               title="{% blocktranslate with name=app.name %}Models in the {{ name }} application{% endblocktranslate %}">
                                {{ app.name }}
                            </a>
                        </h2>
                        {% if app.models %}
                            <table>
                                <thead>
                                    <tr>
                                        <th>النموذج</th>
                                        <th>الإجراءات</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {% for model in app.models %}
                                        <tr>
                                            <td>{{ model.name }}</td>
                                            <td>
                                                {% if model.admin_url %}
                                                    <a href="{{ model.admin_url }}" class="changelink">{% translate 'عرض' %}</a>
                                                {% endif %}
                                                {% if model.add_url %}
                                                    <a href="{{ model.add_url }}" class="addlink">{% translate 'إضافة' %}</a>
                                                {% endif %}
                                            </td>
                                        </tr>
                                    {% endfor %}
                                </tbody>
                            </table>
                        {% endif %}
                    </div>
                {% endfor %}
            </div>
        {% else %}
            <p>{% translate "You don't have permission to view or edit anything." %}</p>
        {% endif %}
    </div>
{% endblock %}
