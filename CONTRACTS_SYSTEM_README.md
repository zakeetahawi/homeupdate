# نظام إدارة العقود - دليل الاستخدام

## 📋 نظرة عامة

تم إضافة نظام شامل لإدارة العقود وتفاصيل الستائر إلى نظام إدارة الطلبات. يتيح هذا النظام:

- إنشاء عقود تلقائية بعد حفظ الطلب
- إدارة تفاصيل الستائر (حتى 20 ستارة لكل عقد)
- توليد ملفات PDF للعقود بتصميم احترافي
- رفع العقود تلقائياً إلى Google Drive
- قوالب قابلة للتخصيص

---

## 🏗️ المكونات الرئيسية

### 1. النماذج (Models)

#### `ContractTemplate` - قالب العقد
- معلومات الشركة (الاسم، الشعار، العنوان، الهاتف، الرقم الضريبي)
- إعدادات التصميم (الألوان، الخطوط، حجم الصفحة)
- محتوى HTML و CSS مخصص
- إعدادات العرض (ما يتم عرضه في العقد)
- نصوص مخصصة (الرأس، التذييل، الشروط والأحكام)

#### `ContractCurtain` - تفاصيل الستارة
- معلومات الغرفة والمقاسات (الاسم، العرض، الطول)
- صورة موديل الستارة
- **القماش والأمتار:**
  - قماش خفيف (النوع، الأمتار، نوع التفصيل، مقاس الشريط)
  - قماش ثقيل (النوع، الأمتار، نوع التفصيل، مقاس الشريط)
  - بلاك أوت (النوع، الأمتار، نوع التفصيل، مقاس الشريط)
- **الإكسسوارات:**
  - خشب (العدد، النوع - يدوي)
  - مجرى (العدد، النوع - من الفاتورة)
  - مواسير (العدد، النوع - من الفاتورة)
  - كوابيل (العدد، النوع - من الفاتورة)
  - نهايات (العدد، النوع - من الفاتورة)
  - طبة (العدد، النوع - من الفاتورة)
  - شماعات (العدد، النوع - من الفاتورة)
  - فرانشة (العدد، النوع - من الفاتورة)
  - شرابة (العدد، النوع - من الفاتورة)
  - مرابط يدوي (العدد، نوع القماش - من الفاتورة)
  - حزام وسط (العدد، النوع - من الفاتورة)
- ملاحظات وشرح

#### `ContractPrintLog` - سجل طباعة العقود
- تسجيل كل عملية طباعة/توليد للعقد
- المستخدم الذي قام بالطباعة
- نوع الطباعة (تلقائي/يدوي)

---

## 🔄 سير العمل التلقائي

### عند إنشاء طلب جديد:

1. **حفظ الطلب والفاتورة** ✅
2. **توليد العقد تلقائياً** (للطلبات من نوع `installation` أو `tailoring`)
   - يتم استدعاء مهمة خلفية `generate_contract_async`
   - توليد ملف PDF من القالب الافتراضي
   - حفظ الملف في حقل `contract_file` في الطلب
3. **رفع العقد إلى Google Drive** (تلقائياً)
   - يتم استدعاء مهمة خلفية `upload_contract_to_drive_async`
   - رفع الملف وحفظ معلومات Drive

---

## 📝 كيفية إدارة الستائر في العقد

### الوصول إلى صفحة إدارة الستائر:

```
/orders/order/<order_id>/contract/curtains/
```

### الحقول المطلوبة لكل ستارة:

1. **معلومات أساسية:**
   - اسم الغرفة
   - العرض (بالمتر)
   - الطول (بالمتر)
   - صورة الموديل (اختياري)

2. **القماش:**
   - لكل نوع (خفيف/ثقيل/بلاك أوت):
     - اختيار القماش من عناصر الفاتورة
     - عدد الأمتار
     - نوع التفصيل (حلقات/شريط/كبس)
     - مقاس الشريط (إذا كان النوع شريط)

3. **الإكسسوارات:**
   - العدد (يدوي)
   - النوع (من قائمة منسدلة من عناصر الفاتورة)

4. **ملاحظات:**
   - حقل نصي حر للملاحظات والشرح

---

## 🎨 تصميم العقد

### المواصفات:
- **حجم الصفحة:** A4 أفقي (Landscape)
- **عدد الستائر في الصفحة:** حتى 4 ستائر
- **الحد الأقصى:** 20 ستارة لكل عقد
- **التصميم:** شبكة 2×2 لكل صفحة

### الأقسام المعروضة:
1. رأس العقد (معلومات الشركة والعقد)
2. بيانات العميل
3. تفاصيل الستائر (بتصميم بطاقات)
4. الشروط والأحكام
5. التوقيعات
6. التذييل

---

## ⚙️ الملفات المضافة

### النماذج:
- `orders/contract_models.py` - نماذج العقود
- `orders/contract_forms.py` - نماذج الإدخال
- `orders/contract_admin.py` - إدارة Django Admin

### الخدمات:
- `orders/services/contract_generation_service.py` - خدمة توليد PDF

### الواجهات:
- `orders/contract_views.py` - صفحات إدارة العقود
- `orders/templates/orders/contract_pdf_template.html` - قالب PDF

### المهام الخلفية:
- `orders/tasks.py` - مهمة `generate_contract_async`

### الأوامر:
- `orders/management/commands/create_default_contract_template.py`

---

## 🚀 الخطوات التالية

### ما تم إنجازه: ✅
1. ✅ إنشاء نماذج العقود والستائر
2. ✅ خدمة توليد PDF
3. ✅ مهام خلفية للتوليد التلقائي
4. ✅ دمج في سير عمل الطلب
5. ✅ قالب PDF بتصميم احترافي
6. ✅ قالب افتراضي في قاعدة البيانات

### ما يحتاج إلى إكمال: 🔨
1. 🔨 إنشاء واجهة ويب لإدارة الستائر
2. 🔨 إنشاء محرر قوالب (مشابه لمحرر الفواتير)
3. 🔨 إضافة زر في صفحة الطلب للوصول لإدارة الستائر
4. 🔨 اختبار النظام الكامل

---

## 📞 ملاحظات مهمة

1. **التحقق من الكميات:** النظام يتحقق تلقائياً من أن الكميات المستخدمة في العقد لا تتجاوز الكميات في الفاتورة

2. **أنواع الطلبات:** العقود تُنشأ تلقائياً فقط لطلبات `installation` و `tailoring`

3. **القالب الافتراضي:** تم إنشاء قالب افتراضي، يمكن تعديله من لوحة التحكم

4. **Google Drive:** يتم رفع العقود تلقائياً إلى Google Drive بعد التوليد

---

## 🎯 الاستخدام السريع

### لإنشاء قالب جديد:
```python
from orders.contract_models import ContractTemplate

template = ContractTemplate.objects.create(
    name='قالب مخصص',
    company_name='اسم الشركة',
    # ... باقي الحقول
)
```

### لتوليد عقد يدوياً:
```python
from orders.services.contract_generation_service import ContractGenerationService

service = ContractGenerationService(order, template)
service.save_contract_to_order(user)
```

---

تم إنشاء هذا النظام بواسطة Augment AI 🤖

