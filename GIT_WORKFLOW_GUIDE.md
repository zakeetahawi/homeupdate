# 🌿 دليل إدارة الفروع في Git

## 📋 سير العمل الموصى به

### 1️⃣ البقاء في فرعك واستقبال تحديثات من main

```bash
# الطريقة السريعة (استخدم السكريبت)
./scripts/sync_with_main.sh

# أو يدوياً:
git fetch origin                 # جلب التحديثات
git merge origin/main           # دمج تحديثات main في فرعك
```

### 2️⃣ رفع تحديثاتك على فرعك الحالي

```bash
# إضافة التغييرات
git add .

# عمل commit
git commit -m "وصف التغييرات"

# رفع على فرعك
git push origin feature/complete-refactoring-and-testing-2026-01-22
```

### 3️⃣ سير العمل الكامل

```bash
# 1. تأكد من فرعك الحالي
git branch

# 2. اجلب آخر تحديثات من main
git fetch origin

# 3. ادمج تحديثات main في فرعك
git merge origin/main

# 4. اعمل تغييراتك
# ... تعديل الملفات ...

# 5. ارفع تغييراتك
git add .
git commit -m "التحديثات الجديدة"
git push

# 6. كرر الخطوات 2-5 حسب الحاجة
```

---

## 🔄 الأوامر الأساسية

### معرفة الفرع الحالي
```bash
git branch                      # عرض جميع الفروع المحلية
git branch -a                   # عرض جميع الفروع (محلية وبعيدة)
git branch --show-current       # عرض الفرع الحالي فقط
```

### التبديل بين الفروع
```bash
git checkout main                                    # الانتقال إلى main
git checkout feature/complete-refactoring-2026-01-22 # الانتقال إلى فرعك
```

### جلب التحديثات
```bash
git fetch origin                # جلب جميع التحديثات
git fetch origin main           # جلب تحديثات main فقط
```

### دمج التحديثات
```bash
git merge origin/main           # دمج main في فرعك الحالي
git merge origin/main --no-edit # دمج بدون فتح محرر
```

### رفع التحديثات
```bash
git push                        # رفع على الفرع الحالي
git push origin BRANCH_NAME     # رفع على فرع محدد
git push -f                     # رفع إجباري (خطر!)
```

---

## 📊 السيناريوهات الشائعة

### السيناريو 1: تحديثات جديدة في main وأنت في فرعك

```bash
# الوضع: أنت في feature/your-branch
# هدف: استقبال تحديثات main

git fetch origin
git merge origin/main

# إذا حدثت تعارضات:
# 1. افتح الملفات المتعارضة
# 2. حل التعارضات يدوياً
# 3. git add .
# 4. git commit
```

### السيناريو 2: تريد رفع تحديثاتك

```bash
# الوضع: عملت تغييرات في فرعك
# هدف: رفعها على GitHub

git add .
git commit -m "وصف التحديثات"
git push
```

### السيناريو 3: تريد تحديث main ثم رفع فرعك

```bash
# 1. استقبل تحديثات main
git fetch origin
git merge origin/main

# 2. حل أي تعارضات إن وجدت

# 3. ارفع فرعك المحدث
git push
```

---

## ⚠️ تجنب هذه الأخطاء

### ❌ خطأ 1: الرفع الإجباري بدون سبب
```bash
git push -f    # خطر! قد تفقد بيانات
```

### ✅ الصحيح:
```bash
git push       # رفع عادي
```

### ❌ خطأ 2: العمل مباشرة في main
```bash
git checkout main
# ... تعديلات ...
git commit
```

### ✅ الصحيح:
```bash
git checkout -b feature/new-feature
# ... تعديلات ...
git commit
```

### ❌ خطأ 3: نسيان جلب التحديثات
```bash
git merge origin/main    # قد لا يكون محدث!
```

### ✅ الصحيح:
```bash
git fetch origin         # جلب أولاً
git merge origin/main    # ثم دمج
```

---

## 🎯 أفضل الممارسات

### 1. استخدم أسماء واضحة للفروع
```bash
✅ feature/user-authentication
✅ bugfix/login-error
✅ hotfix/security-patch
❌ my-branch
❌ test
❌ temp
```

### 2. اعمل commits صغيرة ومتكررة
```bash
✅ git commit -m "Add user login validation"
✅ git commit -m "Fix password hashing bug"
❌ git commit -m "Fixed everything"
```

### 3. اجلب التحديثات بانتظام
```bash
# كل يوم أو قبل البدء بالعمل
git fetch origin
git merge origin/main
```

### 4. اختبر قبل الرفع
```bash
# قبل git push
pytest tests/
python manage.py check
```

---

## 🔧 السكريبتات المساعدة

### sync_with_main.sh
استقبال تحديثات من main مع البقاء في فرعك
```bash
./scripts/sync_with_main.sh
```

### quick_commit.sh (جديد)
```bash
#!/bin/bash
# إضافة وcommit ورفع بسرعة

git add .
git commit -m "$1"
git push

# الاستخدام:
# ./scripts/quick_commit.sh "وصف التحديث"
```

---

## 📈 مخطط سير العمل

```
main (الفرع الأصلي)
  │
  ├─── تحديثات جديدة من الفريق
  │
  ↓ (git fetch + merge)
  │
feature/your-branch (فرعك)
  │
  ├─── تعديلاتك
  │
  ↓ (git push)
  │
origin/feature/your-branch (على GitHub)
  │
  ↓ (Pull Request)
  │
main (بعد الموافقة)
```

---

## 🆘 حل المشاكل الشائعة

### مشكلة: "Your branch is behind"
```bash
# الحل:
git pull origin main
```

### مشكلة: "Merge conflicts"
```bash
# الحل:
# 1. افتح الملفات المتعارضة
# 2. ابحث عن:
#    <<<<<<< HEAD
#    كودك
#    =======
#    كود main
#    >>>>>>> origin/main
# 3. احذف العلامات واختر الكود الصحيح
# 4. git add .
# 5. git commit
```

### مشكلة: "Permission denied"
```bash
# الحل:
# تأكد من SSH key أو استخدم HTTPS
git remote set-url origin https://github.com/user/repo.git
```

---

## 📚 مراجع سريعة

### الأوامر الأساسية
| الأمر | الوصف |
|-------|-------|
| `git status` | حالة الملفات |
| `git log` | سجل الcommits |
| `git diff` | الفروقات |
| `git branch` | إدارة الفروع |
| `git fetch` | جلب التحديثات |
| `git merge` | دمج الفروع |
| `git push` | رفع التحديثات |
| `git pull` | جلب ودمج |

### الاختصارات المفيدة
```bash
# إضافة alias في ~/.gitconfig
[alias]
    st = status
    co = checkout
    br = branch
    ci = commit
    sync = !git fetch origin && git merge origin/main
    
# الاستخدام:
git st      # بدلاً من git status
git sync    # بدلاً من git fetch && git merge
```

---

## ✅ قائمة التحقق اليومية

قبل البدء بالعمل:
- [ ] `git fetch origin`
- [ ] `git merge origin/main`
- [ ] `git status`

أثناء العمل:
- [ ] commits صغيرة ومتكررة
- [ ] رسائل commit واضحة
- [ ] اختبار التغييرات

قبل إنهاء العمل:
- [ ] `git status` (تأكد من عدم وجود ملفات منسية)
- [ ] `pytest tests/`
- [ ] `git push`

---

**تم إنشاء هذا الدليل في: 2026-01-23**
