# 🔧 دليل استكشاف أخطاء تسجيل الدخول

## المشكلة: لا أستطيع تسجيل الدخول رغم أن الجهاز مسجل

### الخطوات التشخيصية:

#### 1️⃣ التحقق من تسجيل الجهاز

افتح صفحة التشخيص:
```
http://your-domain.com/accounts/device-diagnostic/
```

هذه الصفحة ستخبرك:
- ✅ هل الجهاز مسجل؟
- ✅ ما هي بصمة الجهاز الحالية؟
- ✅ ما هو الفرع المرتبط بالجهاز؟

---

#### 2️⃣ التحقق من فرع المستخدم

تأكد من:
1. أن المستخدم لديه فرع محدد
2. أن فرع المستخدم = فرع الجهاز

**كيف:**
- اذهب إلى `/admin/accounts/user/`
- افتح المستخدم
- تحقق من حقل "الفرع"
- قارنه مع فرع الجهاز

---

#### 3️⃣ التحقق من حالة الجهاز

من لوحة الإدارة:
```
/admin/accounts/branchdevice/
```

تأكد أن:
- ✅ الجهاز **نشط** (is_active = True)
- ✅ البصمة صحيحة

---

#### 4️⃣ مشاكل شائعة وحلولها

##### المشكلة: "هذا الجهاز غير مسجل في النظام"

**الأسباب المحتملة:**
1. **البصمة تغيرت** - حدث في المتصفح:
   - تم تحديث المتصفح
   - تم مسح بيانات المتصفح
   - تم تغيير دقة الشاشة
   - تم استخدام متصفح مختلف

**الحل:**
```bash
# 1. احذف التسجيل القديم من لوحة الإدارة
# 2. سجل الجهاز من جديد من نفس المتصفح
```

##### المشكلة: "هذا الجهاز مسجل لفرع X وأنت تنتمي لفرع Y"

**السبب:** المستخدم يحاول الدخول من جهاز فرع آخر

**الحل:**
```bash
# خياران:
# 1. غيّر فرع المستخدم ليطابق فرع الجهاز
# 2. استخدم جهاز من فرع المستخدم
```

##### المشكلة: "لا يمكنك تسجيل الدخول من هذا الجهاز"

**السبب:** الجهاز غير نشط (is_active = False)

**الحل:**
```bash
# من لوحة الإدارة:
# 1. اذهب إلى /admin/accounts/branchdevice/
# 2. اختر الجهاز
# 3. حدد "تفعيل الأجهزة المحددة"
```

---

### 5️⃣ فحص السجلات (Logs)

إذا كنت ما زلت تواجه المشكلة، افحص السجلات:

```bash
# عرض آخر 50 سطر من السجلات
tail -n 50 logs/django.log

# أو ابحث عن محاولات تسجيل الدخول
grep "Login attempt" logs/django.log | tail -n 20

# ابحث عن أخطاء الأجهزة
grep "Device" logs/django.log | tail -n 20
```

---

### 6️⃣ الحالات الخاصة

#### المديرون والسوبر يوزر

✅ **السوبر يوزر (`is_superuser=True`)**: يمكنه الدخول من أي جهاز
✅ **المدير العام (`is_general_manager=True`)**: يمكنه الدخول من أي جهاز

لا تنطبق عليهم قيود الأجهزة!

---

### 7️⃣ إعادة تسجيل الجهاز (الحل النهائي)

إذا فشلت جميع المحاولات:

```bash
# الخطوة 1: احذف الجهاز القديم
# من /admin/accounts/branchdevice/

# الخطوة 2: افتح المتصفح من الجهاز نفسه
# الخطوة 3: اذهب إلى /accounts/register-device/
# الخطوة 4: سجل الجهاز من جديد
```

---

### 8️⃣ الأوامر المفيدة

```bash
# عرض جميع الأجهزة
python manage.py device_stats

# عرض أجهزة فرع محدد
python manage.py device_stats --branch "الادارة"

# عرض الأجهزة غير المستخدمة
python manage.py device_stats --unused
```

---

### 9️⃣ استعلامات مباشرة (للمتقدمين)

```python
# التحقق من تسجيل جهاز بصمة معينة
from accounts.models import BranchDevice
fingerprint = "ea91b4cfba0a1a76..."  # البصمة من صفحة التشخيص
device = BranchDevice.objects.filter(device_fingerprint=fingerprint).first()
if device:
    print(f"الجهاز: {device.device_name}")
    print(f"الفرع: {device.branch.name}")
    print(f"نشط: {device.is_active}")
else:
    print("الجهاز غير مسجل")

# التحقق من فرع مستخدم
from accounts.models import User
user = User.objects.get(username="username_here")
print(f"فرع المستخدم: {user.branch.name if user.branch else 'None'}")
```

---

## ✅ الخلاصة السريعة

**قبل أي شيء، افتح:**
```
/accounts/device-diagnostic/
```

ستخبرك هذه الصفحة بالضبط ما هي المشكلة!

---

**التحديث:** 7 ديسمبر 2025
