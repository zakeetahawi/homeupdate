# ملخص تكامل نظام الويزارد - دليل سريع

## ✅ ما تم إنجازه

تم تطوير نظام تكامل متكامل يجمع بين نظام الويزارد والنظام القديم مع إمكانية التحكم الكامل من لوحة الإدارة.

---

## 📁 الملفات المنشأة

### 1. **نماذج الإدارة**
- `orders/wizard_admin.py` - إدارة مسودات الويزارد
- `orders/admin_settings.py` - إدارة إعدادات النظام
- `orders/models_settings.py` - نموذج الإعدادات

### 2. **الميزات والأدوات**
- `orders/middleware_settings.py` - Middleware للإعدادات
- `orders/templatetags/system_settings_tags.py` - Template tags

### 3. **القوالب**
- `orders/templates/orders/tags/order_create_buttons.html`
- `orders/templates/orders/tags/edit_button.html`

### 4. **الملفات الثابتة**
- `orders/static/admin/js/system_settings.js`
- `orders/static/admin/css/system_settings.css`

### 5. **التوثيق**
- `WIZARD_INTEGRATION_GUIDE.md` - الدليل الشامل

---

## 🎯 الميزات الرئيسية

### ✨ 1. التحكم الكامل في النظام
- اختيار نظام الويزارد فقط
- اختيار النظام القديم فقط
- استخدام كلا النظامين معاً

### ✨ 2. التعديل الذكي
- التعديل حسب طريقة الإنشاء (افتراضي)
- فرض الويزارد دائماً
- فرض النظام القديم دائماً

### ✨ 3. الحقول الديناميكية
- أنواع التفصيل (إضافة/حذف/تعديل)
- أنواع الأقمشة (إضافة/حذف/تعديل)
- أنواع التركيب (إضافة/حذف/تعديل)
- طرق الدفع (إضافة/حذف/تعديل)

### ✨ 4. إدارة المسودات
- عرض تفصيلي للمسودات
- تقدم الخطوات بصرياً
- تحويل إلى طلبات نهائية
- متابعة من لوحة التحكم

---

## 🚀 خطوات التشغيل السريع

### 1. تطبيق Migrations
```bash
cd /home/zakee/homeupdate
python manage.py migrate orders
```

### 2. جمع الملفات الثابتة
```bash
python manage.py collectstatic --noinput
```

### 3. إنشاء الإعدادات الافتراضية
```bash
python manage.py shell
>>> from orders.models_settings import SystemSettings
>>> settings = SystemSettings.get_settings()
>>> print(settings.order_system)
>>> exit()
```

### 4. الوصول لوحة التحكم
- افتح المتصفح: `http://localhost:8000/admin/`
- اذهب إلى: **Orders → System Settings**
- اختر الإعدادات المناسبة
- احفظ التغييرات

---

## ⚙️ إعدادات مقترحة

### للبدء بالويزارد فقط:
```
نظام إنشاء الطلبات: نظام الويزارد فقط
أولوية التعديل: حسب طريقة الإنشاء
إخفاء النظام القديم: ✓
```

### للحفاظ على النظام القديم:
```
نظام إنشاء الطلبات: النظام القديم فقط
أولوية التعديل: حسب طريقة الإنشاء
إخفاء نظام الويزارد: ✓
```

### للمرونة الكاملة:
```
نظام إنشاء الطلبات: كلا النظامين
أولوية التعديل: حسب طريقة الإنشاء
```

---

## 📊 استخدام النظام

### إنشاء طلب جديد

#### في القالب:
```django
{% load system_settings_tags %}

{# عرض أزرار الإنشاء حسب الإعدادات #}
{% show_order_create_buttons %}
```

### تعديل طلب

#### في القالب:
```django
{% load system_settings_tags %}

{# زر التعديل الذكي #}
{% show_order_edit_button order %}

{# أو الحصول على الرابط مباشرة #}
<a href="{% get_edit_url_for_order order %}">تعديل</a>
```

### استخدام الحقول الديناميكية

#### في النماذج:
```django
{% load system_settings_tags %}

<select name="tailoring_type">
    {% for type in ''|get_tailoring_types %}
        <option value="{{ type.value }}">{{ type.label }}</option>
    {% endfor %}
</select>
```

---

## 🔐 الصلاحيات

### مسودات الويزارد:
- **مدير النظام**: جميع المسودات
- **مدير عام**: جميع المسودات
- **مدير منطقة**: مسودات فروعه فقط
- **مستخدم عادي**: مسوداته فقط

### إعدادات النظام:
- **مدير النظام**: تعديل كامل
- **الآخرون**: عرض فقط

---

## 📝 أمثلة برمجية

### الحصول على الإعدادات في View:
```python
from orders.models_settings import SystemSettings

def my_view(request):
    settings = SystemSettings.get_settings()
    
    if settings.is_wizard_enabled():
        # نظام الويزارد مفعّل
        pass
    
    if settings.is_legacy_enabled():
        # النظام القديم مفعّل
        pass
    
    # الحصول على أنواع التفصيل
    tailoring_types = SystemSettings.get_tailoring_types()
```

### إضافة نوع تفصيل جديد برمجياً:
```python
from orders.models_settings import SystemSettings

settings = SystemSettings.get_settings()
settings.tailoring_types.append({
    'value': 'custom_type',
    'label': 'نوع مخصص'
})
settings.save()

# مسح الكاش
SystemSettings.invalidate_cache()
```

---

## 🐛 حل المشاكل الشائعة

### المشكلة: المسودات لا تظهر
**الحل:**
```python
# تحقق من الصلاحيات
draft.created_by == request.user
# أو
draft.branch in request.user.branches.all()
```

### المشكلة: الإعدادات لا تعمل
**الحل:**
```python
# امسح الكاش
from orders.models_settings import SystemSettings
SystemSettings.invalidate_cache()
```

### المشكلة: الأزرار لا تظهر
**الحل:**
1. تحقق من تحميل Template Tags: `{% load system_settings_tags %}`
2. تحقق من جمع الملفات الثابتة: `python manage.py collectstatic`
3. تحقق من الإعدادات في لوحة التحكم

---

## 📞 الدعم

للمساعدة:
1. راجع الدليل الشامل: `WIZARD_INTEGRATION_GUIDE.md`
2. تحقق من السجلات (Logs)
3. اتصل بالدعم الفني

---

## ✅ قائمة التحقق النهائية

- [ ] تطبيق Migrations
- [ ] جمع الملفات الثابتة
- [ ] إنشاء الإعدادات الافتراضية
- [ ] تكوين الإعدادات من لوحة التحكم
- [ ] اختبار إنشاء طلب بالويزارد
- [ ] اختبار إنشاء طلب بالنظام القديم
- [ ] اختبار التعديل الذكي
- [ ] اختبار الحقول الديناميكية

---

**تم بنجاح!** 🎉

النظام جاهز للاستخدام مع تكامل كامل بين الويزارد والنظام القديم.
